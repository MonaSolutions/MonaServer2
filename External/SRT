// To compile MonaBase with SRT:
// On linux => use ENABLE_SRT flag (make CFLAGS="-DENABLE_SRT=1 " LIBS="-lsrt ")
// On Windows => add SRT header in External/include folder and SRT libraries in External/lib/lib64 folders
